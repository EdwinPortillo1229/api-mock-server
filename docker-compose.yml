services:
  api-mock-server:
    build: .
    ports:
      - "${PORT:-4010}:${PORT:-4010}"
    volumes:
      - ./src:/app/src
    environment:
      - PORT=${PORT:-4010}
      - NGROK_ENABLED=false
      # - NGROK_AUTHTOKEN=your_token_here
    command: npx tsx watch src/index.ts
